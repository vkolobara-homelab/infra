apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: monitoring
spec:
  chart:
    spec:
      chart: kube-prometheus-stack
      sourceRef:
        kind: HelmRepository
        name: monitoring
      version: 41.5.1
  values:
    grafana:
      ingress:
        enabled: true
        hosts:
          - monitoring.internal.vkolobara.com
        tls:
          hosts:
            - monitoring.internal.vkolobara.com
  interval: 1m0s
