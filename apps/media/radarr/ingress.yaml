apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: radarr
  labels:
    name: radarr
spec:
  rules:
    - host: radarr.vkolobara.home
      http:
        paths:
          - pathType: Prefix
            path: "/"
            backend:
              service:
                name: radarr
                port:
                  number: 7878
